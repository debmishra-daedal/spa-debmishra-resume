# This file serves as the main docker-compose entry point
# It automatically includes the appropriate configuration based on TRAEFIK_ENV

# Default to development if TRAEFIK_ENV is not set
# Use: TRAEFIK_ENV=dev for development or TRAEFIK_ENV=prod for production

include:
  - path: 
      - docker-compose.${TRAEFIK_ENV:-dev}.yml